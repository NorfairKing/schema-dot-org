loops:
  generate: |
    set -ex
    stack install schema-dot-org-generator --exec='schema-dot-org-generator schemas.json' --no-nix-pure --fast
    stack build schema-dot-org --fast
    cat schema-dot-org/src/SchemaDotOrg.hs
